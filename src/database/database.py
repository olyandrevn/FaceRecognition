class Database:
    def __init__(self, config):
        pass

    def create_table(self, table_name, columns):
        """
        Create a table in the database with specified columns.

        Args:
            table_name (str): Name of the table to create.
            columns (dict): Dictionary of column names and types.

        Returns:
            table: The created table object.
        """
        pass

    def insert_record(self, table_name, values):
        """
        Insert a record into a specified table.

        Args:
            table_name (str): Name of the table to insert the record into.
            values (dict): Dictionary of column-value pairs for the record.

        Returns:
            result: The result of the insertion operation.
        """
        pass